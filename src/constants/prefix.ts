export enum AUTH_ROUTES {
    BASE = 'auth',
    REGISTER = 'register',
    LOGIN = 'login',
}

export enum MARKDOWN_ROUTES {
    BASE = 'markdowns',
    CREATE = '',
    GET_ALL = '',
    DELETE = ':markdownId',
}
